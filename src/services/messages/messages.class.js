const { Service } = require('feathers-nedb');

exports.Messages = class Messages extends Service {
    // create (data, params) {
    //     // This is the information we want from the user signup data..destructure data...
    //     const { btitle, desc } = data;

    //     const  userMessage = {
    //        btitle,
    //        desc
    //     };

    //        return super.create(userMessage, params);
    //     }
};

